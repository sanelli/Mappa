# Mappa.Samples.Aot
This project generate an executable that tests that all the generated code can actually be compiled AOT.

## How to compile AOT
Run the following command: ` dotnet publish -c Release --self-contained ./Mappa.Samples.Aot/
`